#include "main.h"

/**
* _islower(int c) - checks if the character is lowercase
*
* @: a character argument
* this program check "if the character is lowercase"
* Return: returns 1 and 0 depending on conditions
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

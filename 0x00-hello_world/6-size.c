#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - print the string in the put function
*
* Description: using the main function
* this program prints "Programming is like building a multilingual puzzle"
* Return: 0
*/
int main(void)
{
	char c;
	int i;
	long li;
	long long lli;
	float f;

	printf("size of char is=%i bytes\n", sizeof(char));
	printf("size of int is=%i bytes\n", sizeof(int));
	printf("size of long is=%i bytes\n", sizeof(long));
	printf("size of long long is=%i bytes\n", sizeof(long long));
	printf("size of float is=%i bytes\n", sizeof(float));
	return (0);
}
